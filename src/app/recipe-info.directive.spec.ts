import { RecipeInfoDirective } from './recipe-info.directive';

describe('RecipeInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipeInfoDirective();
    expect(directive).toBeTruthy();
  });
});
